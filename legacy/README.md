# Legacy materials (excluded from the formal manuscript)

## `entropy_diagnostics/`

Earlier topic-entropy models and appendix text. **Not** part of the confirmatory set (H1 / H2 / D1). Do not cite as current Appendix results.

Primary frozen topic assignments for the formal paper are carried as `topic_id` in `analysis_ready_with_topics.csv` / `model_data_final.csv`.

## `fig4_engagement_return.py`

Former manuscript Figure 4 (adjusted engagement predictions for indirect/mixed expression). Removed from the current paper figure set; kept only for internal reproduction.

```bash
uv run python legacy/fig4_engagement_return.py --help
```
